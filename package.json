{
  "name": "ip-tracker",
  "version": "1.0.0",
  "description": "Network VLAN IP Tracker",
  "main": "server.js",
  "scripts": {
    "build": "node --check server.js && node --check sophos-sync.js && node --check network-time.js && node --check public/app.js && node --check public/theme.js && node --check public/dashboard.js && node --check public/sophos.js && node --check public/workspace.js && node --check public/analytics.js",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ping": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
